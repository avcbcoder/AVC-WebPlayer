const mediaButtons = document.getElementsByClassName('player-controls')[0].childNodes[0]
const shuffle = mediaButtons.childNodes[0];
const prev = mediaButtons.childNodes[1];
const playPause = mediaButtons.childNodes[2];
const next = mediaButtons.childNodes[3];
const repeat = mediaButtons.childNodes[4];


